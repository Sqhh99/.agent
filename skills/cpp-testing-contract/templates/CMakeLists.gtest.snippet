# Drop into tests/CMakeLists.txt after find_package(GTest CONFIG REQUIRED)
# or FetchContent of googletest.

add_executable(unit_tests
    add_test.cpp
)
target_link_libraries(unit_tests PRIVATE
    GTest::gtest_main
    your_lib
)
include(GoogleTest)
gtest_discover_tests(unit_tests)
